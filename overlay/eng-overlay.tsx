import type { ProductOverlay } from "./types";

/**
 * mynd Engineering overlay.
 *
 * Placeholder: Engineering currently relies entirely on the shared design
 * system + config/eng. Add dashboard widgets or extra routes here only if the
 * standard layouts don't cover a need (keep < 500 LOC).
 */
export const engOverlay: ProductOverlay = {};

export default engOverlay;
